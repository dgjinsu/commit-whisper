spring:
  profiles:
    active: local
  
  datasource:
    url: jdbc:postgresql://localhost:5432/commit-whisper
    username: postgres
    password: rlawlstn
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:}
            client-secret: ${GOOGLE_CLIENT_SECRET:}
            scope:
              - profile
              - email

python:
  script:
    path: scripts/commit_summarizer.py
  executable: python

openai:
  api:
    key: ${OPENAI_API_KEY:}
  mock:
    enabled: true

slack:
  webhook:
    url: ${SLACK_WEBHOOK_URL:}